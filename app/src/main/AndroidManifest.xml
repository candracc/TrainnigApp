<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trainnigapp">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrainnigApp">
        <activity android:name=".Registrar"></activity>
        <activity android:name=".Perfil" />
        <activity android:name=".Plan_alimentacion" />
        <activity android:name=".Dia_entrenamiento" />
        <activity android:name=".Plan_Entrenamiento" />
        <activity android:name=".Grid_Opciones" />
        <activity android:name=".Inicio" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>